name: handler_framework
description: Reusable platform/service handlers for skeleton-based apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.0.3
  geolocator: ^14.0.2
  injectable: ^2.5.1
  internet_connection_checker: ^1.0.0+1
  nfc_manager: ^4.1.1
  permission_handler: ^12.0.1
  sensors_plus: ^5.0.0
  supabase_flutter: ^2.12.0
